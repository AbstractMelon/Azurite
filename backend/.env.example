# Server Configuration
PORT=8080 # This controls what port the backend server listens on.
HOST=localhost # This controls the hostname or IP address the backend server listens on.
ENV=development # This controls the environment the backend server runs in.

# Database Configuration
DB_PATH=./azurite.db # This controls the path to the SQLite database file.

# JWT Secret (change this in production!)
JWT_SECRET=your-super-secret-jwt-key-change-in-production # This controls the secret key used for JWT authentication.

# OAuth Configuration (optional)
GITHUB_CLIENT_ID=your-github-client-id # This controls the client ID for GitHub OAuth. Setup here: https://github.com/settings/developers
GITHUB_CLIENT_SECRET=your-github-client-secret # This controls the client secret for GitHub OAuth. Setup here: https://github.com/settings/developers
DISCORD_CLIENT_ID=your-discord-client-id # This controls the client ID for Discord OAuth. Setup here: https://discord.com/developers/applications
DISCORD_CLIENT_SECRET=your-discord-client-secret # This controls the client secret for Discord OAuth. Setup here: https://discord.com/developers/applications
GOOGLE_CLIENT_ID=your-google-client-id # This controls the client ID for Google OAuth. Setup here: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_SECRET=your-google-client-secret # This controls the client secret for Google OAuth. Setup here: https://console.cloud.google.com/apis/credentials

# File Storage Configuration
MODS_PATH=./storage/mods # This controls the path to the mods directory.
IMAGES_PATH=./storage/images # This controls the path to the images directory.
MAX_FILE_SIZE=104857600 # This controls the maximum file size allowed for uploads. Currently set to 100MB.

# Email Configuration (for notifications and password reset)
SMTP_HOST=smtp.gmail.com # This controls the SMTP host for email notifications and password resets. Currently set to Gmail.
SMTP_PORT=587 # This controls the SMTP port for email notifications and password resets. Currently set to 587 (gmail).
SMTP_USERNAME=your-email@gmail.com # This controls the SMTP username for email notifications and password resets. Setup google app here: https://console.cloud.google.com/apis/credentials
SMTP_PASSWORD=your-app-password # This controls the SMTP password for email notifications and password resets. Setup google app here: https://console.cloud.google.com/apis/credentials
FROM_EMAIL=noreply@azurite.dev # This controls the email address used for sending notifications and password resets. Can be set to whatever you want.
